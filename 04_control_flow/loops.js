const arr = "anish"

const stringarr=arr.split("")
console.log(stringarr)

const myObject = {
    game1: 'NFS',
    game2: 'Spiderman'
}

for (const key in myObject) {
    const element = object[key];   
    
}

const map = new Map()
map.set("in","india")
map.set("usa","united states of america")

for(const [key,value] of map){
    console.log(key,value)
}

const hehe = [2,3,1,4]

for (const i in hehe) {
  console.log(i)
  
}